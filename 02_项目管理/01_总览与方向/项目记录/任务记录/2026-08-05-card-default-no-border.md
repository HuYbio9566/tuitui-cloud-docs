# 卡片默认无描边修复记录（2026-08-05）

用户要求：默认卡片只显示底色，不显示描边；hover 时才出现主题色描边。

已统一覆盖知识库、技能、专家、连接器、工具、云盘文件卡片、推荐卡片和首页动作卡片：

- 默认 border 设为 transparent，默认无阴影。
- hover 显示 #7B82F9 蓝紫主题描边。
- hover 增加轻量蓝紫阴影，保持可感知但不突兀。
- 已同步当前首页、工作稿、public、dist/client 和 1.3.1 快照。
- npm run build 通过。

